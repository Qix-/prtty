language: cpp

compiler:
  - clang
  - gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.9
      - clang-3.4

before_script:
  - $CXX --version
  - mkdir build
  - cd build
  - cmake ..

script:
  - cmake --build .
  - ./prtty_tests ../test
